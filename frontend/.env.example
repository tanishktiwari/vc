# Frontend Environment Variables
# For local development, copy this to .env.local and use localhost
# For production, update these to your Render backend URL

# Backend API URL (HTTP/HTTPS)
# For Render deployment, use: https://vc-cep7.onrender.com
REACT_APP_API_URL=https://vc-cep7.onrender.com

# WebSocket URL (WS/WSS)
# For Render deployment, use: wss://vc-cep7.onrender.com
# Note: Use 'wss://' (secure WebSocket) for HTTPS sites
REACT_APP_WS_URL=wss://vc-cep7.onrender.com
